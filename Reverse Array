/*
Pseudocode:

1. Start
2. Declare function prototypes for:
   a. Creating and initializing an array with random values
   b. Printing the contents of an array
   c. Reversing the contents of an array
3. In the main function:
   a. Declare integer variables for:
      - Array size
      - Random seed
   b. Loop until a valid array size is entered:
      - Prompt user for array size
      - If the size is invalid (<= 0), display an error message and repeat
   c. Prompt user for the seed value and store it
   d. Create an array using the entered size and seed
   e. If memory allocation fails, print an error message and exit
   f. Print the original array
   g. Reverse the array
   h. Print the reversed array
   i. Deallocate the dynamically allocated memory
   j. End program

4. Function `createArray(size, seed)`:
   a. If size is invalid (<= 0), return nullptr
   b. Seed the random number generator
   c. Allocate memory for an array of the given size
   d. Traverse the array using a pointer and populate it with random numbers between 0 and 19
   e. Return the allocated array

5. Function `printArray(arr, size)`:
   a. If the array is invalid or size is <= 0, return
   b. Iterate through the array using a pointer
   c. Print each element separated by a space
   d. Print a newline at the end

6. Function `reverseArray(arr, size)`:
   a. If the array is invalid or size is <= 0, return
   b. Use two pointers: one starting from the beginning and one from the end of the array
   c. Swap elements using the pointers and move them towards each other
   d. Continue until the start pointer crosses the end pointer

7. End
*/


#include <iostream>
#include <iomanip>
#include <cstdlib>

using namespace std;

// Function to create and initialize an array with random values

int* createArray(int size, int seed);

// Function to print the contents of an array

void printArray(int* arr, int size);

// Function to reverse the contents of an array

void reverseArray(int* arr, int size);

int main() 
{
    // Declare integer variables for array size and random seed
    
    int size, seed;
    
    // Get and validate array size
    
    do 
    {
        // Prompt user for array size and store it in the size variable
        
        cout << "Enter the size of the array: ";
        cin >> size;

        // Check if the entered size is invalid
         
        if (size <= 0) 
        {
            // Inform the user of invalid input
            
            cout << "Array size must be greater than 0." << endl;
        }
    } while (size <= 0);    // Repeat until a valid size is entered
    
    // Prompt user for random seed value and read seed value from user input
    
    cout << "\nEnter the seed value: ";
    cin >> seed;

    // Create array of given size using values entered by user
    
    int* arr = createArray(size, seed);

    // Check if memory allocation was successful
    
    if (!arr) 
    {
        // Print error message if allocation fails
        
        cout << "Memory allocation failed" << endl;
        return 1;
    }
    
    // Print original array
    
    printArray(arr, size);
    
    // Reverse the array
    
    reverseArray(arr, size);
    
    // Print the reversed array
    
    printArray(arr, size);

    // Free allocated memory to prevent memory leaks
    
    delete[] arr;
    
    return 0;
}

int* createArray(int size, int seed) 
{
    // Ensure size is valid before proceeding
    
    if (size <= 0) 
    {
        // Return null
        
        return nullptr;
    }
    
    // Seed the random number generator
    
    srand(seed);
    
    // Allocate memory for the array dynamically
    
    int* arr = new int[size];
    
    // Create pointer to traverse and fill the array
    
    int* ptr = arr;
    
    // Populate the array with random values in the range 0 to 19
    
    for (int i = 0; i < size; i++) 
    {
        // Assign a random value to the current position
        
        *ptr = rand() % 20;  
        
        // Move pointer to the next position
        
        ptr++;
    }
    
    // Returning arr
    
    return arr;
}

// Function to print the elements of an array

void printArray(int* arr, int size) 
{
    // Ensure the array is valid before printing
    
    if (!arr || size <= 0) 
    {
        // Exit function if the array is null
        
        return;
    }

    // Create a pointer to iterate over the array
    
    int* ptr = arr;
    
    // Loop through the array elements
    
    for (int i = 0; i < size; i++) 
    {
        // Print the value pointed to by ptr
        
        cout << *ptr;
        
        // Check if this is not the last element
        
        if (i < size - 1) 
        {
            // Print a space
            
            cout << " ";
        }
        
        // Move to the next element
         
        ptr++;
    }
    
    // Print newline after printing all elements
    
    cout << " " << endl;
}

// Function to reverse the elements of an array

void reverseArray(int* arr, int size) 
{
    // Ensure the array is valid before proceeding
    
    if (!arr || size <= 0) 
    {
        // Exit function if array is null 
        
        return;
    }
    
    // Pointer to the first element
    
    int* start = arr;
    
    // Pointer to the last element
    
    int* end = arr + size - 1;
    
    // Loop until start pointer crosses end pointer
    
    while (start < end) 
    {
        // Swap elements using pointers
        
        int temp = *start;
        *start = *end;
        *end = temp;
        
        // Move start pointer forward
        
        start++;
        
        // Move end pointer backward
        
        end--;
    }
}
